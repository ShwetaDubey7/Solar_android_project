<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.solaro">

    <!-- Permissions for Location and Camera -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Solaro">

        <!--
          FIX: Splash Activity is declared with its full path and set as the LAUNCHER.
          The '.' in the name is a shortcut for the package name defined above.
        -->
        <activity android:name=".app.activity.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare all other activities using the '.' shortcut -->
        <activity android:name=".app.MainActivity" />
        <activity android:name=".app.activity.LoginActivity" />
        <activity android:name=".app.activity.RegisterActivity" />
        <activity android:name=".app.activity.UserDashboardActivity" />
        <activity android:name=".app.activity.AdminDashboardActivity" />
        <activity android:name=".app.activity.RequestInstallationActivity" />
        <activity android:name=".app.activity.ManageInstallationsActivity" />
        <activity android:name=".app.activity.ManageUsersActivity" />
        <activity android:name=".app.activity.RequestMaintenanceActivity" />
        <activity android:name=".app.activity.ManageMaintenanceActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
