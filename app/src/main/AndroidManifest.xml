<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.solaro">

    <!-- Permissions for Location and Camera -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Solaro"
        tools:ignore="DataExtractionRules">
        <!-- Splash Activity -->
        <activity
            android:name=".app.activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".app.activity.AboutActivity" android:exported="false"/>
        <activity android:name=".app.activity.LoginActivity" android:exported="false"/>
        <activity android:name=".app.activity.RegisterActivity" android:exported="false"/>
        <activity android:name=".app.activity.UserDashboardActivity" android:exported="false"/>
        <activity android:name=".app.activity.AdminDashboardActivity" android:exported="false"/>
        <activity android:name=".app.activity.RequestInstallationActivity" android:exported="false"/>
        <activity android:name=".app.activity.ManageInstallationsActivity" android:exported="false"/>
        <activity android:name=".app.activity.ManageUsersActivity" android:exported="false"/>
        <activity android:name=".app.activity.RequestMaintenanceActivity" android:exported="false"/>
        <activity android:name=".app.activity.ManageMaintenanceActivity" android:exported="false"/>
        <activity android:name=".app.activity.ManageProductsActivity" android:exported="false" />
        <activity android:name=".app.activity.ShopActivity" android:exported="false" />
        <activity android:name=".app.activity.PaymentActivity" android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
